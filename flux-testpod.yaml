apiVersion: v1
kind: Pod
metadata:
  name: flux-test-pod
  namespace: target-namespace
  labels:
    name: flux-test-pod
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80